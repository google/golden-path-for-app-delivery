kind: Deployment
apiVersion: apps/v1
metadata:
  name: gceme-backend
spec:
  selector:
    matchLabels:
      app: gceme
      role: backend
      env: base
  template:
    metadata:
      name: backend
      labels:
        app: gceme
        role: backend
        env: base
    spec:
      securityContext:
        runAsUser: 1000
        runAsNonRoot: true
      containers:
      - name: backend
        image: sample-app-backend
        resources:
          limits:
            memory: "128Mi"
            cpu: "250m"
        env:
        - name: VERSION
          value: base
        imagePullPolicy: Always
        readinessProbe:
          initialDelaySeconds: 2
          periodSeconds: 5
          httpGet:
            path: /healthz
            port: 8080
        ports:
        - name: backend
          containerPort: 8080
