apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gceme-frontend
  annotations:
    kubernetes.io/ingress.class: "gce-internal"
spec:
  defaultBackend:
    service:
      name: gceme-frontend-staging
      port:
        number: 8080