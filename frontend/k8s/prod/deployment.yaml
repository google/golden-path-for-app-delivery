kind: Deployment
apiVersion: apps/v1
metadata:
  name: gceme-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      env: production
  template:
    metadata:
      labels:
        env: production
    spec:
      containers:
      - name: frontend
        image: sample-app-frontend
        env:
        - name: VERSION
          value: production
        - name: BACKEND_URL
          value: http://gceme-backend-prod:8080
        resources:
          limits:
            memory: "128Mi"
            cpu: "250m"
