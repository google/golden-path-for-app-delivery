kind: Deployment
apiVersion: apps/v1
metadata:
  name: gceme-frontend
spec:
  selector:
    matchLabels:
      app: gceme
      role: frontend
      env: base
  template:
    metadata:
      name: frontend
      labels:
        app: gceme
        role: frontend
        env: base
    spec:
      securityContext:
        runAsUser: 1000
        runAsNonRoot: true
      containers:
      - name: frontend
        image: sample-app-frontend
        env:
        - name: VERSION
          value: base
        resources:
          limits:
            memory: "128Mi"
            cpu: "250m"
        imagePullPolicy: Always
        readinessProbe:
          initialDelaySeconds: 2
          periodSeconds: 5
          httpGet:
            path: /healthz
            port: 8080
        ports:
        - name: frontend
          containerPort: 8080
