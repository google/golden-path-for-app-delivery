apiVersion: deploy.cloud.google.com/v1beta1
kind: DeliveryPipeline
metadata:
  name: web-app
  annotations:
    vic.dev/owner: "viglesias@google.com"
  labels:
    app: web-app
description: web-app delivery pipeline
serialPipeline:
  stages:
  - targetId: staging
    profiles:
    - staging
  - targetId: canary
    profiles:
    - canary
  - targetId: prod
    profiles:
    - prod
